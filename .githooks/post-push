#!/bin/bash
# StockPulz post-push hook
# Summarizes what changed and saves a pending release note to the Gist.
# Setup: git config core.hooksPath .githooks

cd "$(git rev-parse --show-toplevel)"
echo "[post-push] Generating release note..."
python3 scripts/on_push.py
