anthropic>=0.25.0
yfinance>=0.2.40
pandas>=2.0.0
ta>=0.11.0
requests>=2.31.0
flask>=3.0.0
gunicorn>=21.0.0
pytz>=2024.1
lxml>=4.9.0
html5lib>=1.1
mplfinance>=0.12.10b0
matplotlib>=3.7.0
aiohttp>=3.9.0
supabase>=2.0.0
upstash-redis>=1.0.0
# Explicit pins: anthropic + supabase + upstash-redis all need httpx; without
# these top-level pins pip 25.x backtracks into ResolutionImpossible on Render.
httpx>=0.27,<0.29
httpcore>=1.0,<2
sentry-sdk[flask]>=2.0.0
finnhub-python>=2.4.0
curl_cffi>=0.7.0
# Needed by tests that parse .github/workflows/*.yml (e.g. test_selfheal_triage.py).
# Without this pinned, pytest's collection of that module fails at import time,
# which aborts the ENTIRE `pytest tests/ -q` run (no --continue-on-collection-errors) —
# so a missing test-only dependency silently breaks the whole suite, including
# self_heal.yml's merge gate.
pyyaml>=6.0
